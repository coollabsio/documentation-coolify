---
title: Installation
description: Explore Coolify’s features, integrations, guides, troubleshooting tips, and usage steps.
icon: Rocket
full: false
showEdit: true
showLastUpdated: true
showRate: true
---

import { Accordion, Accordions } from 'fumadocs-ui/components/accordion';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Cpu, Microchip, Database, ArrowUpDown, Cog, Radiation, CircleDashed, User, KeyRound, CircleUserRound, BookMarked, Code, LifeBuoy, AlertTriangle, Info, Layers, Server, Box, Download, Folder, HardDrive, Play, Settings, Shield, Terminal } from "lucide-react";
import { Unplug, ServerCrash, Cloud } from "lucide-react";
import { ImageCard } from "@/components/image-card";
import { ImageCardGroup } from "@/components/image-card-group";
import { Step, Steps } from 'fumadocs-ui/components/steps';

# Installation
If you decide to go with **Coolify Cloud**, there's no installation required. 

Simply visit [Coolify Cloud Registration](https://app.coolify.io/register) to create an account and start using Coolify within minutes!

Below, you'll find instructions for installing Coolify if you prefer to **self-host** it.

---

## Self-hosted Installation
If you like taking control and managing everything yourself, self-hosting Coolify is the way to go. 

It's completely free (apart from your server costs) and gives you full control over your setup.

---

## Before You Begin
Before installing Coolify, make sure your server meets the necessary requirements.

<Tabs items={['Hardware', 'Operating System', 'CPU Architecture', 'Real-World Usage'] } updateAnchor>

  <Tab id="tab-hardware" value="Hardware">
    <Callout type="tip">
      We recommend using a **fresh server** without any pre-installed apps to **avoid conflicts** with Coolify. 
    
      While Coolify can run on lower-spec servers, we suggest slightly higher resources to avoid resource issues after deploying a few apps.
    </Callout>
    <Details className="mt-5">
      <Detail label="CPU" value="2 Cores" icon={<Cpu />} />
      <Detail label="Memory" value="2 GB RAM" icon={<Microchip />} />
      <Detail label="Storage" value="30 GB" icon={<Database />} />
      <Detail label="Access" value="Root User SSH" icon={<CircleUserRound />} />
      <Detail label="Network" value="IPv4 or IPv6 - (IPv4 recommended)" icon={<ArrowUpDown />} />
      <Detail label="Server Type" value="VPS, Dedicated Server, Bare Metal, VM, Rasberry Pi, etc." icon={<Server />} />
    </Details>
  </Tab>

  <Tab id="tab-os" value="Operating System">
    <Callout type="tip">
      Coolify is built to run on Linux Operating System. If you like to run Coolify on operating system like macOS or Windows, then you have to setup a Virtual machine and install coolify there

      Below are the Operating Systems the coolify community reported to be working fine for Coolify.
    </Callout>
    <Details className="mt-5">
      <Detail label="Debian-based" value="Debian, Ubuntu" icon={<CircleDashed />} />
      <Detail label="RedHat-based" value="CentOS, Fedora, AlmaLinux, etc." icon={<CircleDashed />} />
      <Detail label="SUSE-based" value="SUSE, openSUSE" icon={<CircleDashed />} />
      <Detail label="Other" value="Arch, Alpine, Raspberry Pi OS (64-bit)" icon={<CircleDashed />} />
    </Details>
  </Tab>

  <Tab id="tab-cpu-arch" value="CPU Architecture">
    <Callout type="tip">Coolify supports 64-bit CPUs only.</Callout>
    <Details className="mt-5">
      <Detail label="CPU Type" value="AMD64, ARM64" icon={<Cpu />} />
    </Details>
  </Tab>

  <Tab id="tab-rw-example" value="Real-World Usage">
    <Callout type="success">**Andras Bacsai** (The founder of Coolify) runs his production apps on a server with the following setup.</Callout>
    <Details className="mt-5">
      <h3 className="mt-0">Server Specifications</h3>
      <Detail label="CPU" value="4 cores (avg 20–30%)" icon={<Cpu />} />
      <Detail label="Memory" value="8 GB (avg 3.5 GB used)" icon={<Microchip />} />
      <Detail label="Storage" value="150 GB (avg 40 GB used)" icon={<Database />} />
      <h3 className="mt-5">Deployed Applications</h3>
      <Detail label="Coolify" value="v5.0.0-beta.2" icon={<CircleDashed />} />
      <Detail label="3 Node.js apps" value="Next.js, Nuxt, Astro etc.." icon={<CircleDashed />} />
      <Detail label="4 Static sites" value="Fully Static (No SSR)" icon={<CircleDashed />} />
      <Detail label="Plausible" value="A open-source Analytics tool" icon={<CircleDashed />} />
      <Detail label="Fider" value="A open-source Feedback tool" icon={<CircleDashed />} />
      <Detail label="UptimeKuma" value="A open-source Monitoring tool" icon={<CircleDashed />} />
      <Detail label="Ghost" value="A open-source Newsletter tool" icon={<CircleDashed />} />
      <Detail label="3 Redis databases" value="A in-memory database" icon={<CircleDashed />} />
      <Detail label="2 PostgreSQL databases" value="A open-source SQL database" icon={<CircleDashed />} />
    </Details>
  </Tab>

</Tabs>



## Installation

Coolify can be installed in several ways depending on your platform and preferences.

<Tabs items={['Automated', 'Manual', 'Rasberry PI', 'Hetzner', 'Oracle', 'AWS']} updateAnchor>

  <Tab id="tab-automated" value="Automated">
    <Callout type="success">
       Automatically installs Docker and Coolify with minimal setup. 
       
       (Recommended for most users)
    </Callout>
    <Steps>
    <Step>
      **Prepare Your Server**
      - Log in as the root user (non-root users are not fully supported yet).
      - Set up your firewall with the help of the [Firewall Guide](/knowledge-base/server/firewall).
      - Ensure that [curl](https://curl.se/) is installed (it usually comes pre-installed).
    </Step>
    <Step>
      **Run the Installation Script**

      Once your server is ready, run:
      ```sh
      curl -fsSL https://cdn.coollabs.io/coolify/install.sh | bash
      ```
    <Callout type="tip">
      If you're not logged in as the root user, run the script with sudo:

      ```sh
      curl -fsSL https://cdn.coollabs.io/coolify/install.sh | sudo bash
      ```
    </Callout>
    <Accordions type="single">
      <Accordion title="Learn more about the Installer">
        #### What the Installer Does: [!toc]
        - Installs essential tools (curl, wget, git, jq, openssl)
        - Installs Docker Engine (version 24+)
        - Configures Docker settings (logging, daemon)
        - Sets up directories at `/data/coolify`
        - Configures SSH keys for server management
        - Installs and starts Coolify

        #### Source Code [!toc]
        - View the [Script's Source Code](https://github.com/coollabsio/coolify/blob/main/scripts/install.sh)

      </Accordion>
      <Accordion title="Tip for advanced users">
        You can also set up the first admin account directly during the installation. For details, see: [Create Root User with Environment Variables](/knowledge-base/create-root-user-with-env)
        There are several environment variables that you can set to customize your Coolify installation.

        For example, you can setup a default root user or define the default docker network pool.

        See the [Define Custom Docker Network with ENV ](/knowledge-base/define-custom-docker-network-with-env) or the [Create Root User with ENV](/knowledge-base/create-root-user-with-env) for more details.
      </Accordion>
    </Accordions>
    </Step>
    <Step>
      **Access Coolify**

      After installation, the script will display your Coolify URL (e.g., `http://203.0.113.1:8000`). 
      
      Visit this URL, and you'll be redirected to a registration page to create your first admin account.

      <Callout type="warn">
      **Immediately create your admin account after installation.**
      
      If someone else accesses the registration page before you, they might gain full control of your server.
    </Callout>
    </Step>
    </Steps>
  </Tab>

  <Tab id="tab-manual"value="Manual">
    <Callout type="warn">
      Advanced method. Gives full control over the setup but requires manual Docker, directory, and environment config.
    </Callout>
    <Details className="mt-5">
      <Detail label="Docker" value="Install manually (24+ required)" icon={<Box />} />
      <Detail label="Directory" value="/data/coolify must exist" icon={<Folder />} />
      <Detail label="Env Setup" value="Manually set .env values" icon={<Settings />} />
      <Detail label="Launch" value="docker compose up -d" icon={<Play />} />
    </Details>
  </Tab>

  <Tab id="tab-rpi"value="Rasberry PI">
    <Callout type="warn">
      Advanced method. Gives full control over the setup but requires manual Docker, directory, and environment config.
    </Callout>
    <Details className="mt-5">
      <Detail label="Docker" value="Install manually (24+ required)" icon={<Box />} />
      <Detail label="Directory" value="/data/coolify must exist" icon={<Folder />} />
      <Detail label="Env Setup" value="Manually set .env values" icon={<Settings />} />
      <Detail label="Launch" value="docker compose up -d" icon={<Play />} />
    </Details>
  </Tab>

  <Tab id="tab-hetzner"value="Hetzner">
    <Callout>
      Hetzner is our recommended hosting provider. You can deploy pre-configured servers quickly.
    </Callout>
    <Details className="mt-5">
      <Detail label="Recommended Image" value="Ubuntu 22.04 Minimal" icon={<HardDrive />} />
      <Detail label="SSH Access" value="Enable during server creation" icon={<KeyRound />} />
      <Detail label="Firewall" value="Allow ports 22, 80, 443" icon={<Shield />} />
      <Detail label="Install" value="Use Standard method or our prebuilt images" icon={<Download />} />
    </Details>
  </Tab>

  <Tab id="tab-oracle"value="Oracle">
    <Callout type="info">
      Oracle Free Tier is compatible with Coolify but may require swap space due to 1GB RAM limitation.
    </Callout>
    <Details className="mt-5">
      <Detail label="OS Image" value="Ubuntu 22.04 LTS" icon={<HardDrive />} />
      <Detail label="Memory" value="1GB – Add swap manually" icon={<Microchip />} />
      <Detail label="Install" value="Standard method works with curl" icon={<Terminal />} />
    </Details>
  </Tab>

  <Tab id="tab-aws"value="AWS">
    <Callout type="info">
      Coolify works well on AWS EC2 instances. Choose an instance with at least 2GB RAM.
    </Callout>
    <Details className="mt-5">
      <Detail label="Recommended Type" value="t3.small or larger" icon={<Cpu />} />
      <Detail label="OS Image" value="Ubuntu 22.04" icon={<HardDrive />} />
      <Detail label="Firewall (SG)" value="Allow ports 22, 80, 443" icon={<Shield />} />
      <Detail label="Install" value="Run Standard install via curl" icon={<Terminal />} />
    </Details>
  </Tab>
</Tabs>
