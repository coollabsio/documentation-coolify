# All these data are dummy value so don't panic okay?

COOLIFY_FQDN="coolify.shadowarcanist.pvt"
COOLIFY_TRIGGER_UUID="fks0g40co4s43vs0c80w4sg"
COOLIFY_TRIGGER_API_URL="https://${COOLIFY_FQDN}/api/v1/deploy?uuid=${COOLIFY_TRIGGER_UUID}&force=true"

# For polling deployments, we use the list endpoint.
COOLIFY_LIST_API_URL="https://${COOLIFY_FQDN}/api/v1/deployments"
# Use the unique application UUID that appears in the deployment URL.
COOLIFY_TARGET_APP_UUID="$COOLIFY_TRIGGER_UUID"

# API token
COOLIFY_API_TOKEN="69|BGmTgn5bv16MVnr45RrIWR23sf3fdi62c93138c"

# Discord Webhook URLs and alert ping
DISCORD_WEBHOOK_SUCCESS="https://discord.com/api/webhooks/133622480155200/upk6lUa587MILEWnbHPEw92AHdDnUTA0FEnsnRiROHbc0-2U8n1X1BHJad3YaA0BaA?thread_id=133629051233474347"
DISCORD_WEBHOOK_FAILURE="https://discord.com/api/webhooks/133622480155200/upk6lUa587MILEWnbHPEw92AHdDnUTA0FEnsnRiROHbc0-2U8n1X1BHJad3YaA0BaA?thread_id=133629051233474347"
DISCORD_ALERT_PING="<@&13278799202424829896>"

# VPS and SSH configuration
VPS_IP="192.168.69.69"
VPS_PORT=69
VPS_USERNAME="coolify"
SSH_KEY_FILE="/data/coolify/.ssh/dev-instance"

# Docker image configuration
IMAGE_NAME="coolify-docs-v5"
DOCKER_TAG="staging"

# Deployment polling configuration (timeout in seconds and poll interval)
DEPLOY_POLL_TIMEOUT=120    # Maximum time to wait (in seconds)
DEPLOY_POLL_INTERVAL=1    # Time between each poll (in seconds)

# Runner configuration
RUNNER_TARGET_NAME="Staging"

# Get the absolute path to the directory containing this script
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

# Determine the root of the project (2 levels up from this script)
PROJECT_ROOT="$(cd "$SCRIPT_DIR/../.." && pwd)"

# Dockerfile path (relative to project root)
DOCKERFILE_PATH="$PROJECT_ROOT/deployment/Dockerfile"